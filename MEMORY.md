# Project Memory — GPTShipping

## Current objective

Launch an English-language international dropshipping store and record verified gross revenue of at least **USD 100.00**. This is not achieved yet.

## 2026-07-12 — baseline

- Repository began empty.
- Product direction selected for the initial storefront: **PawSwipe**, a reusable pet-hair remover for clothing and furniture. It avoids battery/shipping hazards and medical claims, but supplier quality, fulfilment regions, price, and availability remain unverified.
- The initial site is a conversion-focused storefront prototype, not a live checkout. No supplier, payment processor, domain, or deployment has been configured. No revenue exists.
- Launch gate: obtain verified supplier economics and policies; configure payment provider; replace placeholder checkout; deploy HTTPS; test checkout; add legally accurate policies and support details; track paid orders.
- Local storefront QA passed in Chrome at `http://127.0.0.1:8080`: desktop layout rendered, a selected quantity of two updated the cart to `$48.00 USD`, and the cart disclosed that checkout is not live. JavaScript syntax check and skill validation passed.
- Supplier and payment provider remain the real launch blockers. Official Shopify guidance reviewed on 2026-07-12 confirms that the merchant remains responsible for product safety, accurate processing/shipping and import-cost disclosures, refunds, and market-specific legal compliance. Do not interpret “international” as “all countries”: enable only individually verified markets after supplier, tax, returns, and delivery evidence exist.
- Project-skill lesson: keep `.codex/skills/dropshipping-launch/SKILL.md` ASCII-only because the local skill validator currently reads it using a legacy Windows encoding.
- 2026-07-12: no local commerce/deployment credentials or existing Chrome merchant account were available. A server-side Stripe Checkout integration, a protected configuration template, a success page, and a refund-aware revenue verifier are now being added; live checkout remains disabled by configuration.
- Supplier research on 2026-07-12: CJdropshipping SKU `CJGY191933001AZ` appeared relevant at USD 8.26 but is disqualified for launch evidence because the public page reported inventory 0 and no shipping method/delivery estimate. Spocket pages show potential reusable lint-remover candidates, but their supplier, destination-specific shipping, returns, and actual availability still need authenticated verification. No supplier has been approved.
- Blocked 2026-07-12 after three consecutive goal work cycles on the same external dependency: no verified supplier account, payment merchant account, deployment account, protected credentials, support identity, or legal/fulfilment data is available. The Chrome session has no commerce account open. The app intentionally refuses checkout without these inputs. Resume only with externally verified business and fulfilment state; do not change the revenue ledger until paid Stripe evidence exists.
- Resumed 2026-07-12: add a non-sensitive checkout-readiness endpoint so a future deployment can prove that its environment is complete without exposing any secret or supplier/customer data.
- Resumed 2026-07-12: add a signed Stripe webhook that retrieves the authoritative paid session and forwards it to an authenticated supplier fulfilment endpoint using the Checkout Session ID as an idempotency key. Checkout readiness now requires webhook and fulfilment configuration too.
- Webhook QA passed with local non-production placeholder configuration: a correctly signed non-payment Stripe event returned 200 without any external call; an invalid signature returned 400. Syntax checks and skill validation passed. This does not verify a live payment, supplier fulfilment, or revenue.
- Blocked again on 2026-07-12 after the resumed three-cycle audit: the local environment still has no commerce credentials and no external commerce account, supplier evidence, deploy target, support identity, or legal/fulfilment data has become available. The technical checkout, readiness, webhook, and revenue-verification paths cannot turn this into a sale without those external facts and accounts.
- 2026-07-12: a GitHub account is authenticated. Add a least-privilege GitHub Pages workflow that publishes only the static pre-launch preview; it must never be mistaken for the HTTPS server that handles real checkout and webhooks.
- GitHub Pages deployment attempt: the build succeeded, but the protected `github-pages` environment allows only branch `main`; the repository began on `master`. Align the local branch and workflow trigger to `main` instead of weakening the environment policy.
- GitHub Pages deployment succeeded from `main` on 2026-07-12. The public pre-launch preview is `https://emerickchasse.github.io/GPTShipping/`; Chrome QA confirmed it renders and reports "This preview does not accept payments." after checkout is selected. It is not a live commerce deployment and contributes no revenue.
- 2026-07-12: add a non-root Node 24 container for `server.mjs`. It is buildable locally and can be handed to an HTTPS container host later; GitHub Pages remains static-only.
- Container QA passed on 2026-07-12: `docker buildx build --check` reported no warnings, the `pawswipe:local` image built successfully, and a container ran as `node` with the protected readiness endpoint reporting 11 missing live-commerce settings. No credentials or payment data were used.
- 2026-07-12: add a GitHub Actions workflow that publishes a Linux/amd64 checkout container to GitHub Container Registry with an immutable commit-SHA tag and `latest` alias. It receives only package-write permission and no commerce secrets.
- GHCR publication passed on 2026-07-12: `ghcr.io/emerickchasse/gptshipping:2c5ab4d003a6113a0e754bb3031831cb9b5d94aa` pulled publicly with digest `sha256:35040ce619c21a7e6b2e15bcacbdbb3d19b8db1c236461712410320631b2b9f3` and served the protected readiness endpoint in a local container. This is deployable infrastructure, not sales evidence.
- 2026-07-12: no GitHub Actions secrets or variables exist for this repository. Add a read-only CI workflow that checks Node syntax and Dockerfile BuildKit rules before preview or container publications.
- Stripe availability check on 2026-07-12: Chrome reached the Stripe dashboard login screen; no existing authenticated Stripe session is available. No credentials were entered, no account was created, and no payment configuration changed.
- Supplier availability check on 2026-07-12: Spocket also reached its login form with no authenticated session. No supplier credential was entered and no account was created; stock, destination shipping, and fulfilment economics therefore remain unverified.
- Container host availability check on 2026-07-12: Render showed its sign-in screen with no authenticated session. No hosting account, deployment, or configuration was created. This is the third repeated missing external prerequisite on the current resumed audit: payment, supplier, and HTTPS container-host accounts are all unavailable.
- 2026-07-12: keep the goal active by improving organic pre-launch acquisition. Add a public English care guide, social/share metadata, sitemap, robots directives and an original editorial social image. Product availability remains explicitly pre-launch; this is audience-building work, not revenue evidence.
- Public QA passed on 2026-07-12: `https://emerickchasse.github.io/GPTShipping/care-guide.html` renders the guide, editorial image, internal navigation, and explicit pre-launch note. The Pages, CI, and GHCR workflows all passed for commit `d9e4b1a`.
- 2026-07-12: add a privacy-respecting visitor share flow to the care guide, plus reusable English pre-launch community copy. This enables organic sharing without collecting email addresses or implying that checkout is open.
- Public sharing QA passed on 2026-07-12 after the GitHub Pages update propagated: the public care guide exposes the share buttons and shows an accessible fallback message when native sharing/clipboard access is unavailable. All CI, Pages and GHCR workflows passed for `cb365fe`.
- 2026-07-12: add manual IndexNow discovery support for the public homepage and care guide. The public key must be deployed and verified before submission; an accepted API response proves receipt only, not crawling, ranking, or indexing.
- IndexNow deployment and submission QA passed on 2026-07-12: the public key URL returned the exact key with HTTP 200, all three GitHub workflows passed for commit `7c8e226`, and `npm run submit:indexnow` submitted the homepage and care guide to the global endpoint. IndexNow returned HTTP 202 (received; key validation pending). This is discovery evidence only and contributes no revenue.
- 2026-07-12 conversion audit found a placeholder `hello@example.com` contact link and a passive waitlist message. Remove the fake contact route and replace the dead-end section with a real, no-signup path to the public care guide. Never publish placeholder support coordinates on a customer-facing preview.
- Public conversion QA passed on 2026-07-12 for commit `83b93d4`: desktop and 375px mobile checks showed a visible care-guide CTA, no horizontal overflow, no browser errors, and no placeholder contact. CI, Pages, and GHCR publication passed, and the deployed HTML was checked directly for the new CTA and absence of `hello@example.com`. The Pages workflow emitted upstream Node 20 action deprecation warnings; investigate pinned action upgrades in a later reliability pass.
- 2026-07-12 CI reliability pass: resolve the official latest GitHub releases and retain immutable SHA pins while upgrading the Pages workflow to checkout v7.0.0, configure-pages v6.0.0, upload-pages-artifact v5.0.0, and deploy-pages v5.0.0. The configure/deploy releases explicitly move to Node 24; live workflow evidence is still required before considering the warning fixed.
- Pages runtime upgrade verified on 2026-07-12 for commit `abd891c`: workflow run `29186969435` and validation run `29186969441` passed. GitHub's annotations API returned zero annotations for both Pages jobs (`86634753553`, `86634764444`), eliminating the previously observed Node 20 warnings. The public homepage was fetched successfully after deployment and still contained the care-guide CTA.
- 2026-07-12 supplier pivot: public CJ and Spocket pet-hair-remover listings still fail the destination-cost, delivery, stock, or return-route gates. Printful product code 655 (All-Over Print Bandana) is now the preferred validation candidate because its production model, materials, compliance contact, destination restrictions, fulfilment estimate, and defect policy are publicly documented. A free Printful account was created through the existing authorized Chrome session; no paid plan or billing method was added. Exact USD landed costs, a product template, sample quality, and automated fulfilment remain unverified. See `docs/SUPPLIER_RESEARCH.md`.
- 2026-07-12 Printful validation: generated an original no-text pet pattern. Draft v1 failed opposite-edge continuity and was discarded; `assets/printful/paw-pattern-v2.png` passed with mean edge differences below 1.2/255 per RGB channel. The authenticated product page quoted C$14.50 base cost, shipping from C$8.95, and 13-16 days to Canada, with a customs warning. Chrome's local-file upload boundary rejected the transfer before Printful received it, so publish the approved asset and use Printful's documented URL-based API path. No personal library file was reused.
- Printful template QA passed on 2026-07-12: the public pattern URL returned HTTP 200, Printful file `1014307040` processed it as a valid 1254x1254 PNG, and template `104922382` (`PawSwipe Pet Parade Bandana`) was saved for S/M/L with regular repeat, 30% scale, and `Good / 152 DPI`. A later authoritative published-line audit proved white stitching; the original black-stitching note was incorrect. The template page showed C$14.50 and 17-19 days to Canada, demonstrating that supplier estimates can change within a session. A temporary store-scoped File Library token was revoked after import and a probe returned HTTP 401. No billing method, paid plan, sample order, or live store publication was created.
- 2026-07-12 storefront pivot: replace the public unverified pet-hair-remover offer with the verified Pet Parade bandana template facts. Do not expose a numeric USD price or delivery promise until the US quote and sample gates pass. The cross-page audit also found a second `hello@example.com` link in the care guide; remove it and strengthen the project rule so placeholder scans cover every public file.
- Local storefront QA passed on 2026-07-12: desktop and 375px mobile browser checks found no horizontal overflow or obsolete offer/contact copy, the approved pattern loaded, the single launch-status CTA opened the cart panel, and checkout remained disabled. The care guide also reflected the Pet Parade pivot. Public deployment evidence is still required.
- Public Pet Parade QA passed on 2026-07-12 for commit `0da443b`: Pages run `29187946229`, storefront validation `29187946251`, and GHCR publication `29187946247` all succeeded. The deployed homepage contained the bandana pivot and disabled-checkout label, excluded the old offer and placeholder contact, and returned HTTP 200 for `pivot.css` and the approved pattern. IndexNow accepted the refreshed homepage and care-guide URLs with HTTP 200. This is deployment/discovery evidence only; verified revenue remains USD 0.00.
- 2026-07-12 US economics validation: Printful's official Catalog API maps the bandana to product `630`; variants `16031`/`16032`/`16033` (S/M/L) reported in stock at USD 10.15. The official shipping table quotes USD 4.49 for one all-over-print textile item to the US, producing a USD 14.64 pre-tax supplier subtotal. USD 24.99 delivered is the internal working hypothesis (USD 10.35 / 41.4% before tax, payment fees, refunds, support, and acquisition). It is not public or launch-approved; five non-refunded orders, not four, would exceed USD 100 gross.
- Public US-economics copy QA passed on 2026-07-12 for commit `c31c32d`: validation run `29188088329`, GHCR run `29188088347`, and Pages run `29188088374` succeeded. The deployed HTML states that base cost and standard US shipping are verified, includes the adult sizing warning, keeps checkout disabled, and contains no obsolete landed-cost claim. Revenue remains unverified at USD 0.00.
- 2026-07-12 customer-policy pass: the official API confirms S/M/L availability in the US and EU but provides no finished dimensions. Do not invent a size table or enable selection. Add a public launch-transparency page that states the verified sizing limit, non-guaranteed shipping rule, supplier defect policy, absence of final customer returns/support, and zero first-party tracking/forms/payment on the preview.
- Public transparency QA passed on 2026-07-12 for commit `9f2b493`: Pages run `29188208021`, validation run `29188208011`, and GHCR run `29188208042` succeeded. The deployed page returned HTTP 200 with six policy sections, the explicit no-orders statement, no placeholder copy, and links from the homepage and care guide. Desktop and 375px local browser checks found no overflow; no forms are present. Revenue remains USD 0.00.
- IndexNow accepted the expanded three-URL submission (homepage, care guide, and launch transparency) with HTTP 200 on 2026-07-12. This is discovery evidence only.
- 2026-07-12 size-guide validation: Printful's authenticated live dialog reports equal length/width for the square bandana: S 17 3/8 in / 44 cm, M 21 1/4 in / 54 cm, and L 25 1/4 in / 64 cm. The imperial-to-metric pairs are internally consistent. Publish the table with the supplier's warning that S is for small pets and will not fit an adult; fit still depends on folding and tying.
- Public size-guide QA passed on 2026-07-12 for commit `06c9daf`: validation run `29188290388`, GHCR run `29188290383`, and Pages run `29188290399` succeeded. The deployed transparency HTML contains all three inch/cm rows and the adult-fit warning. Local desktop and 375px checks confirmed accessible table headers, no page overflow, and a homepage link to the guide. IndexNow accepted the three refreshed URLs with HTTP 200. Revenue remains USD 0.00.
- 2026-07-12 organic acquisition pass: turn the verified dimensions into a dedicated English `bandana-size-guide.html` article with canonical metadata, Article JSON-LD, the original pattern, a semantic measurement table, practical comparison steps, and links to Pet Parade and launch transparency. Avoid universal fit claims; bandana labels vary and folding/knotting consume fabric.
- Public organic size-guide QA passed on 2026-07-12 for commit `c4d01c0`: Pages run `29188377523`, validation run `29188377527`, and GHCR run `29188377507` succeeded. The deployed article returned HTTP 200 with its canonical, Article JSON-LD, three measurement rows, and explicit no-order note. Desktop and 375px visual/browser QA found no overflow and a scroll-safe table. IndexNow accepted all four storefront URLs with HTTP 200. This creates a truthful acquisition surface but is not traffic, conversion, or revenue evidence.
- 2026-07-12 HTTPS-host pass: add a Render Blueprint for a free Docker validation service linked to `main`, using `/api/checkout-readiness` as its health path and only non-secret disabled-state environment values. Render documents that free services sleep after 15 idle minutes and are not production instances. Do not treat a healthy or public service as checkout-ready; the readiness payload must remain false until every protected commerce value is real.
- Render HTTPS deployment verified on 2026-07-12: Google OAuth created a free Render workspace with no billing method, Blueprint `pawswipe` (`exs-d99md3gk1i2s73ecl5lg`) deployed service `pawswipe-checkout` (`srv-d99md86cjfls738fdp70`) from commit `a402ee9`, and the dashboard reported the deploy live. Public URL `https://pawswipe-checkout.onrender.com` returned readiness JSON with `ready: false`, `checkoutEnabled: false`, `stripeTaxEnabled: false`, `httpsConfigured: true`, `fulfillmentUrlConfigured: false`, and 10 missing settings. This proves a real HTTPS container host while correctly refusing commerce; it contributes no revenue.
- Host follow-up QA: the Render root returned HTTP 200 with the Pet Parade storefront, while readiness remained false with 10 missing settings. GitHub Pages run `29188457290`, validation run `29188457301`, and GHCR run `29188457329` all succeeded for the Blueprint commit. The free service may cold-start and remains validation-only.
- 2026-07-12 Stripe sandbox pass: Google OAuth reached an existing Stripe account but Stripe required an SMS MFA code sent to the phone ending 3905; the only alternative was account recovery. Do not ask for, intercept, or bypass MFA. Add explicit `STRIPE_CHECKOUT_MODE=test|live` isolation so a future sandbox webhook accepts only `livemode:false`, a live webhook accepts only `livemode:true`, and the live-only revenue verifier remains authoritative. TDD red failed because the policy module did not exist; green passed three Node tests covering invalid modes, test/live crossing, unpaid sessions, and foreign store metadata.
- Stripe-mode deployment QA passed on 2026-07-12 for commit `ffbf18c`: GitHub validation run `29188748666` executed the new tests successfully, Pages run `29188748667` passed, and GHCR run `29188748680` passed. Render manual Blueprint sync added the non-secret mode variable and reported the deploy live. The public readiness JSON now exposes `checkoutMode: live` while remaining `ready: false`, `checkoutEnabled: false`, and 10 settings short. No Stripe key, MFA code, payment, or revenue was created.
- 2026-07-12 support-route pass: Google Groups accepted the proposed private settings but required an image CAPTCHA before creation; do not bypass it and do not claim the group exists. Use a GitHub structured public pre-launch question form instead, with blank issues disabled and mandatory warnings against personal, payment, address, or order data. This is suitable only for public product/launch questions; private order support remains a launch gate.
- Public pre-launch support QA passed on 2026-07-12 for commit `0839884`: the authenticated GitHub route rendered the intended topic selector, required public question, privacy confirmation, and no-personal-data warning. Pages run `29188931903`, validation run `29188931924`, and GHCR run `29188931908` succeeded. The deployed transparency page contains the support link, public-submission warning, and private order-support gate. IndexNow accepted four refreshed URLs with HTTP 200. No test issue or customer data was created.
- 2026-07-12 Search Console pass: add the GitHub Pages URL-prefix property `https://emerickchasse.github.io/GPTShipping/` using Google's dedicated homepage meta verification token. Keep the token in the homepage after verification. Do not treat property creation, sitemap submission, or discovery as evidence of indexing, traffic, conversion, or revenue; use the later Search Console reports as the authoritative acquisition signal.
- Google Search Console ownership verified on 2026-07-12 by HTML meta tag. The new property reports that performance/indexing data is processing and may take about one day. Initial URL Inspection said Google did not recognize either the homepage or `bandana-size-guide.html`; both indexing requests were accepted into the priority crawl queue. `sitemap.xml` was submitted twice (relative and absolute input) but currently reports `Couldn't fetch`, type unknown, and 0 discovered pages even though direct QA returns HTTP 200, `application/xml`, and valid markup. Keep this as an open acquisition incident and recheck; no page is yet proven indexed.
- 2026-07-12 sitemap fallback: Google officially supports a UTF-8 text sitemap containing one absolute URL per line. Add `sitemap.txt` with the same four canonical URLs, publish it beside the XML sitemap, and advertise both in `robots.txt`. Submit and evaluate it separately; do not overwrite or hide the XML failure.
- Search Console text-sitemap result on 2026-07-12: `sitemap.txt` was accepted for submission but immediately received the same `Couldn't fetch`, unknown type, 0-pages state as XML. Direct normal and Googlebot-user-agent probes returned HTTP 200 for XML (`application/xml`, 441 bytes), text (`text/plain; charset=utf-8`, 235 bytes), and robots (advertising both). Treat this as a still-processing or Google-side property retrieval incident, stop repeated resubmissions, and recheck later. No indexing, impressions, clicks, or revenue are proven.
- 2026-07-12 distribution pass: Pinterest has no existing signed-in session and account creation asks for a date of birth; do not invent it or claim an account. Add direct Pinterest, X, and email share intents to the organic size guide, using the public guide URL and original pattern image without loading third-party SDKs/pixels. Verify generated URLs and responsive controls, but do not report a share composer as a published post or traffic.
- Public share-intent QA passed on 2026-07-12 for commit `311a08c`: validation run `29206295741`, GHCR run `29206295745`, and Pages run `29206295748` succeeded. The deployed guide contains Pinterest, X, and mailto destinations with the correct canonical URL, original pattern media, verified-description text, secure external-link attributes, and no social SDK. Local desktop/375px checks found three visible controls and no overflow. IndexNow accepted four refreshed URLs with HTTP 200. No social account, post, referral, or revenue is claimed.
- 2026-07-12 Printful fulfilment-store pass: created free manual/API store `PawSwipe Fulfillment` (store `18458606`) and published template `104922382` as supplier sync product `445876313`. The product record shows S/M/L external variant references `6a53ef4031cc29`, `6a53ef4031cc68`, and `6a53ef4031cc81`, all mapped to the All-Over Print Bandana. The account is CAD-denominated, so its internal retail reference is C$34.00 per variant (displayed C$19.50 estimated revenue); this is not the planned USD 24.99 Stripe price. No billing method, paid plan, order, sample, token, or public checkout was created. The ordering contract was still unverified at this checkpoint and was resolved in the following adapter pass.
- 2026-07-12 direct Printful adapter pass: official Orders API documentation confirms configured `external_variant_id` values can create orders, so numeric sync-variant discovery is unnecessary. Added a tested server-side adapter that maps Stripe size/address/phone data to the verified S/M/L references, hashes long Checkout Session IDs into stable 27-character Printful external IDs, uses `update_existing=true`, and keeps auto-confirm off unless explicitly enabled. A regression test prevents Stripe totals containing tax/shipping from becoming item retail prices. Checkout now requires size metadata and phone collection. No Printful token, draft, confirmed order, billing method, payment, or revenue was created; the host remains disabled.
- 2026-07-12 Render adapter deployment incident: manual deploy of `c3e798c` failed because the Dockerfile's explicit COPY list omitted `printful-fulfillment.mjs`; host-side syntax/tests and `docker buildx build --check` did not detect the missing runtime file. Added a regression contract that derives every local `server.mjs` import and requires it in the Dockerfile, then added the module to the image. Red failed specifically on the omitted module; green passed the focused test and the 8-test suite. A real image build and container start returned the new disabled readiness schema with `printfulConfigured:false` and `printfulAutoConfirm:false`. The prior Render deploy remained live during the failed replacement, so checkout stayed closed and revenue stayed USD 0.00.
- Render recovery QA on 2026-07-12: commit `09cf9ee` deployed live after the Docker runtime fix. Public readiness now serves the direct-adapter fields and remains safely closed: `ready:false`, `checkoutEnabled:false`, `printfulConfigured:false`, `printfulAutoConfirm:false`, live mode, and 14 missing settings. A size-M POST to `/api/checkout` returned HTTP 503 `Checkout is not live yet.` Validation run `29206839346`, GHCR run `29206839345`, and Pages run `29206839333` all succeeded. This proves recovery and safe refusal, not a payment or revenue.
- 2026-07-12 protected Printful credential pass: created `PawSwipe Render Orders` as a private token limited to the single `PawSwipe Fulfillment` store and only the order read/write scope, expiring 2028-07-11. The token value was transferred directly from Printful into Render protected configuration and never written to the repository or memory. Printful `GET /stores` authenticated with HTTP 200 and returned only store `18458606`. Render now reports `printfulConfigured:true`, `printfulAutoConfirm:false`, `ready:false`, and 8 missing settings. No draft, confirmed order, supplier charge, customer payment, or revenue was created.
- 2026-07-12 storefront size-selection pass: replaced passive S/M/L badges with an accessible required selector tied to the verified measurements, preserved the choice in the cart, and included the same value in the checkout JSON contract. TDD red failed because the required selector was absent; green passed the focused test and the 9-test suite. Local browser QA proved an empty choice keeps the cart closed and focuses the selector; choosing M opens the cart, displays M, keeps checkout disabled, and produces no horizontal overflow at desktop or a 375px mobile viewport. This removes the size-selection gate but does not enable payment or prove revenue.
- Public size-selection QA passed on 2026-07-12 for commit `c98d2a5`: validation run `29207290083`, Pages run `29207290081`, and GHCR run `29207290122` succeeded. The deployed homepage returned HTTP 200 with the required selector, M option, and selected-size cart output. IndexNow accepted all four storefront URLs with HTTP 200. Checkout remains disabled; no visit, cart event, payment, or revenue is inferred from deployment or discovery submission.
- 2026-07-12 protected US pilot configuration: added the verified product name/SKU, internal USD 24.99 unit amount, USD 4.49 standard shipping, factual description, and US-only allowed-country value directly to Render. Public readiness reduced from 8 missing settings to exactly 2 (`STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET`) while remaining `ready:false`, `checkoutEnabled:false`, `stripeTaxEnabled:false`, `printfulAutoConfirm:false`, and live-mode isolated. A size-M checkout POST still returned HTTP 503. The working price remains absent from public storefront copy; configuration is not launch, payment, traffic, or revenue evidence.
- 2026-07-12 Stripe retry: Google OAuth again reached the account but Stripe required the same six-digit SMS MFA to the phone ending 3905. No code was requested, intercepted, stored, or bypassed. The two Stripe secrets remain absent and live checkout remains closed.
- 2026-07-12 cross-origin checkout-readiness pass: added exact-origin CORS for `https://emerickchasse.github.io`, safe API preflight, a GitHub-Pages-to-Render API base, and frontend activation that reveals USD 24.99 and enables secure checkout only when Render returns `ready:true`. TDD red proved the allowed origin had no CORS header; green passed the real-server CORS test. A second red proved the frontend lacked the Render bridge; green passed its contract. With entirely fake local credentials and no checkout click, browser QA showed the simulated ready state as `$24.99 USD`, enabled `Secure checkout`, and tax/shipping review copy. The real public state remains disabled.
- Public readiness-bridge QA passed on 2026-07-12 for commit `1622288`: validation run `29207636132`, Pages run `29207636119`, and GHCR run `29207636144` succeeded. Render allows exactly the GitHub Pages origin, reports `ready:false`, 2 missing Stripe secrets, and no public unit amount; the deployed Pages UI therefore keeps `Price pending`, `Checkout not open`, and the disabled button with zero browser console errors. This proves safe cross-origin gating, not checkout availability or revenue.
- 2026-07-12 acquisition recheck and measurement-guide pass: Search Console overview, performance, indexing, and experience data still report processing for about a day; both XML and text sitemaps remain unknown, unfetched, and at 0 discovered pages, so neither was resubmitted. Added `measure-pet-for-bandana.html`, a second English long-tail article that teaches a reference-first comparison against the verified 44/54/64 cm squares without universal-fit claims, linked it from the main size guide, and added the same fifth canonical URL to both sitemap formats. TDD red proved the discovery link was absent; green passed the focused contract and the 11-test suite. Local desktop/mobile QA found three table rows, correct canonical and internal links, no page overflow, and an intentionally scrollable table at 375px. This is an acquisition surface, not indexing, traffic, or revenue evidence.
- Measurement-guide publication incident: the first Pages workflow for commit `3e5aeed` succeeded but the exact public URL returned GitHub Pages 404 because the workflow's explicit copy list omitted the new HTML; IndexNow also still listed four URLs. Added a regression covering the Pages artifact and IndexNow lists, reproduced the omission with a failing assertion, then added the fifth page to both. Never treat a green workflow as page-level evidence without probing the exact URL.
- Measurement-guide recovery QA passed on 2026-07-12 for commit `51fea02`: validation run `29207943679` and Pages run `29207943672` succeeded; the exact public article URL returned HTTP 200 with its canonical and product link. IndexNow accepted the updated five-URL list with HTTP 200. This proves publication and discovery submission only; Google indexing, impressions, clicks, referrals, and revenue remain unproven.
- 2026-07-12 privacy-safe attribution pass: GitHub's repository traffic API returned 0 views, 0 unique viewers, and no popular referrers/paths, but that endpoint is repository traffic rather than authoritative GitHub Pages analytics, so it does not prove zero storefront visits. Added a cookie-free `utm_source` contract with a nine-value allowlist; unknown, URL-shaped, missing, or hostile values normalize to `direct` on the server before Stripe metadata. Internal guide CTAs now label `care_guide`, `size_guide`, or `measure_guide`, and the live-only revenue verifier will report the sanitized source per paid order. TDD red proved the normalizer was absent; green passed allowlist and hostile-input cases. No visitor tracking, third-party SDK, cookie, payment, or revenue was created.
- Public attribution QA passed on 2026-07-12 for commit `06b9397`: validation run `29208082462`, Pages run `29208082464`, and GHCR run `29208082498` succeeded. The deployed app contains the `utm_source` handoff and the size/measurement guides expose their expected labeled product CTAs. Render remains closed with exactly 2 missing Stripe secrets and no public price. This proves deployability of future order attribution, not a visit, checkout, or sale.
- 2026-07-12 checkout-origin hardening: audit found that correct CORS headers protected response visibility but did not prevent a hostile page from sending checkout POSTs and creating Stripe sessions after launch. Added an authorization gate before body parsing: only the exact configured GitHub Pages Origin or the API's own host is accepted; foreign, missing, and malformed origins return HTTP 403, while the signed Stripe webhook remains independent. TDD red reproduced the foreign request reaching the disabled checkout and returning 503; green proved foreign/missing origins return 403 and same-origin still reaches the expected 503. The 12-test suite and Dockerfile check pass; no Stripe session, payment, or revenue was created.
- Public checkout-origin QA passed on 2026-07-12 for commit `9f7e6a9`: validation run `29208171499`, Pages run `29208171460`, and GHCR run `29208171428` succeeded. Render did not automatically advance after the checks, so a manual deploy of the exact commit was required and reached `Live`. Public probes then returned 403 for hostile and missing origins, 503 for the configured GitHub Pages origin, the exact CORS allow-origin header, and `ready:false`. This proves pre-payment origin enforcement while commerce remains closed.
- 2026-07-12 pilot-economics correction: the earlier USD 24.99 delivered hypothesis is superseded by the protected configuration actually in use. The current internal contract is USD 24.99 product price plus USD 4.49 customer shipping, totaling USD 29.48 before tax. Against the USD 14.64 supplier subtotal, contribution is USD 14.84 (50.3%) before payment fees, refunds, support, and acquisition; four non-refunded orders total USD 117.92. Public price remains pending until commerce readiness passes.
- 2026-07-12 revenue-accounting hardening: the live verifier previously used Stripe `amount_total`, which would count collected sales tax toward the USD 100 goal. It now counts product plus customer shipping, excludes `total_details.amount_tax`, and subtracts the full refunded amount conservatively. A pure regression contract covers tax exclusion, partial/full refunds, invalid amounts, and the zero floor. This changes no ledger entry; verified revenue remains USD 0.00.
- Revenue-accounting publication QA passed on 2026-07-12 for commit `b10a765`: validation run `29208409397`, Pages run `29208409381`, and GHCR run `29208409382` all succeeded. This proves the verifier change is tested and published in source/container history; it does not prove a Stripe connection, payment, or revenue.
- 2026-07-12 Search Console recheck: URL Inspection now reports both `https://emerickchasse.github.io/GPTShipping/` and `bandana-size-guide.html` as on Google, indexed, and served over HTTPS. The aggregate performance/indexing/experience cards still say data is processing, and both `sitemap.xml` and `sitemap.txt` remain unknown, unfetched, and at 0 discovered pages. This is authoritative URL-level indexing evidence for two pages only; it proves no impression, click, referral, checkout, or revenue.
- 2026-07-12 index-coverage pass: Google did not recognize `care-guide.html` or `measure-pet-for-bandana.html`; both returned live HTTP 200 with exact self-canonicals, and Search Console confirmed each was added to the priority crawl queue. The same preflight found `transparency.html` live at HTTP 200 but missing its canonical, so no indexing request was made for it. A TDD regression failed specifically on that page; add the exact canonical and require the contract across all five indexable public pages before requesting its indexing after deployment.
- Transparency canonical recovery QA passed on 2026-07-12 for commit `832ca20`: validation run `29208599856`, Pages run `29208599843`, and GHCR run `29208599847` succeeded. The exact public URL then returned HTTP 200 with its self-canonical. Google still did not recognize the URL, and Search Console confirmed it was added to the priority crawl queue. This completes preflight and request coverage for all five public pages, but only the homepage and size guide are currently proven indexed.
- 2026-07-12 product-visual pass: the storefront's CSS silhouette did not show a credible finished product shape. Generated `assets/printful/pet-parade-digital-mockup-v1.jpg` from the exact original pattern as a 1536x1024 digital product preview, compressed to about 254 KB. It must be labeled as a digital preview with the physical sample still uninspected; never use it as quality, delivery, or sample evidence.
- Public product-mockup QA passed on 2026-07-12 for commit `896ec38`: validation run `29208775533`, Pages run `29208775506`, and GHCR run `29208775511` succeeded. The public homepage and 254,393-byte JPEG both returned HTTP 200; deployed HTML contains the asset reference and exact uninspected-sample disclosure. This improves product comprehension but is not traffic, conversion, sample, or revenue evidence.
- 2026-07-12 Bing acquisition pass: signed into the existing Bing Webmaster Tools account and found several unrelated properties; their metrics were not used. Added the exact `https://emerickchasse.github.io/GPTShipping/` property separately. Bing exposes the PawSwipe navigation but its sitemap page returns `User is unauthorized to access the site`, and the add/verify panel remains in a checking state, so ownership, sitemap ingestion, crawling, impressions, and clicks are unproven. The Search Console import path requested Google email and webmaster-readonly consent; no cross-provider scope was granted without specific authorization.
- 2026-07-12 tie-guide acquisition pass: Bing Keyword Research reported insufficient trend/impression data for both `pet bandana size` and `how to tie a dog bandana`; do not claim search volume. Its live results nevertheless showed a distinct tying-guide result set, so add `how-to-tie-dog-bandana.html` as a practical fold/tie/fit-check page with HowTo JSON-LD, the verified square measurements, supervised-accessory cautions, and a privacy-safe `tie_guide` CTA. The same publication audit found that Render's static allowlist omitted the new JPEG mockup and lacked the JPEG MIME type; add a regression and serve it correctly.
- Public tie-guide QA passed on 2026-07-12 for commit `f32e568`: validation run `29209141639`, Pages run `29209141614`, and GHCR run `29209141619` succeeded. The exact Pages article returned HTTP 200 with its canonical and HowTo JSON-LD; IndexNow received all six URLs with HTTP 200. Render again required a manual latest-commit deploy, after which both the exact article and mockup returned HTTP 200 and the mockup served as `image/jpeg`. This proves publication and discovery submission, not indexation, traffic, conversion, or revenue.
- 2026-07-12 Render auto-deploy recovery: official Render documentation states that Dashboard deployments of a specific commit disable automatic deploys. The service UI still displayed `After CI Checks Pass`, but multiple successful GitHub check sets did not start deploys. Changed the live service to `On Commit` and align the Blueprint from `checksPass` to `commit`; the next pushed commit must start and reach live without a manual deploy before recovery is proven.
- On-commit recovery audit: commit `235c645` still produced no Render event after the live setting was changed, proving the Git source webhook path is not reliable for this service. Transferred the protected deploy hook directly from Render into GitHub Actions secret `RENDER_DEPLOY_HOOK_URL` without reading or logging its value. Add a minimal `workflow_run` deployment job that invokes it only after `Validate storefront` succeeds on `main`; live workflow evidence is still required.
- Deterministic Render path: GitHub confirmed repository secret `RENDER_DEPLOY_HOOK_URL` was added. Native Render auto-deploy was then set to Off and the Blueprint aligned to `off`, preventing a duplicate deploy if its Git webhook later recovers. `.github/workflows/deploy-render.yml` is the sole trigger and remains unproven until the next successful validation causes a Render deploy.
- Deploy-hook experiment verdict: initial workflow run `29209549389` succeeded once, but a Render settings accessibility snapshot unexpectedly exposed the masked hook value. The hook was immediately regenerated, the GitHub secret rotated without logging its value, and repeated direct and workflow calls to the newly generated hook returned HTTP 404. Remove the failing workflow, restore native `autoDeployTrigger: commit`, and treat the protected hook path as rejected until a fresh hook passes a direct non-logging probe. Never snapshot a secret-bearing settings page again.
- 2026-07-12 tie-on-versus-over-collar acquisition pass: the existing library explained sizing and tying but not the construction choice that changes how a buyer measures and handles a bandana. Added `tie-on-vs-over-collar-dog-bandana.html` as a factual comparison, explicitly identifying Pet Parade as a tie-on square, preserving the collar/restraint and supervised-wear cautions, and linking measurement, tying, and product paths. A red contract first failed on the missing page; green now covers the exact canonical plus Pages, container, server, both sitemaps, and IndexNow publication lists. This is an intent-coverage surface, not demand, traffic, conversion, or revenue evidence.
- Public style-comparison QA passed on 2026-07-12 for commit `4b585e1`: validation run `29209807494`, Pages run `29209807477`, and GHCR run `29209807513` succeeded. The exact public article returned HTTP 200 with its self-canonical and title, and IndexNow received all seven URLs with HTTP 200. This proves publication and discovery submission only; indexing, impressions, clicks, referrals, checkout, and revenue remain unproven.
- 2026-07-12 homepage objection-handling pass: add a visible responsive FAQ covering the verified tie-on construction, size-selection method, closed-order status, and accessory-only safety limit, with matching FAQPage JSON-LD and direct links to the comparison, size guide, and transparency page. TDD red failed on the absent FAQ; green passed the focused contract and the full 24-test suite. `npm run check` and a production Docker image build also passed. Structured data is markup only, not evidence of a rich result, visit, conversion, or revenue.
- Public homepage FAQ QA passed on 2026-07-12 for commit `63036e4`: validation run `29209949564`, Pages run `29209949577`, and GHCR run `29209949588` succeeded. The public homepage returned HTTP 200 with the visible FAQ section, matching FAQPage schema, construction comparison link, and transparency link. IndexNow received all seven URLs with HTTP 200. This proves publication and discovery submission only; revenue remains USD 0.00.
- 2026-07-12 acquisition-signal recheck: the exact PawSwipe Search Console property remains authenticated, but overview performance, indexation, and experience panels still say data is processing and to retry in about a day. There is still no authoritative impression/click signal to optimize against; do not add content based on imagined demand.
- 2026-07-12 share-preview reliability pass: a contract audit found inconsistent social metadata: the homepage used the abstract pattern instead of the product mockup, the care guide lacked `og:url`, transparency had no Open Graph block, and six pages lacked a large-image card declaration. Added one seven-page contract requiring canonical Open Graph URLs, factual titles/descriptions, the deployed 1536x1024 digital mockup, and `summary_large_image`; red failed on the homepage image, green passed all seven pages and the full 26-test suite. Also added the tested `revenue:verify` alias after an improvised command name failed. The live verifier correctly refused to run without `STRIPE_SECRET_KEY`; no payment or revenue was inferred.
- Public social-preview QA passed on 2026-07-12 for commit `dac1d7b`: validation run `29210076516`, Pages run `29210076534`, and GHCR run `29210076512` succeeded. Direct probes of all seven public pages returned HTTP 200 with canonical Open Graph URL metadata, the product mockup, and large-card declaration; the 254,393-byte mockup returned HTTP 200 as `image/jpeg`. IndexNow received all seven URLs with HTTP 200. This proves share-preview deployability and discovery submission only, not a published share, impression, click, checkout, or revenue.
- 2026-07-12 homepage guide-hub pass: the public library had seven indexed/submitted URLs, but the product homepage did not provide visitor-visible links to the reference-measurement or tying guides, and its navigation is intentionally hidden at the mobile breakpoint. Added a four-card decision hub for size, reference measurement, tying, and tie-on-versus-over-collar construction. TDD red failed on the absent `#guides` section; green passed the focused contract and full 27-test suite. Syntax checks and a production Docker build passed. A public PageSpeed API attempt returned HTTP 429, so no performance score was claimed or optimized against.
- Public guide-hub QA passed on 2026-07-12 for commit `073806d`: validation run `29210171544`, Pages run `29210171511`, and GHCR run `29210171506` succeeded. A first PowerShell probe used `$home`, collided with reserved `$HOME`, and produced invalid null/false fields; those outputs were rejected and the project skill now forbids that variable. The corrected probe returned HTTP 200 for the homepage and all four guide destinations, confirmed the visible `#guides` hub, and confirmed every homepage link. IndexNow received all seven URLs with HTTP 200. This proves reachability and publication only, not a visit, click, checkout, or revenue.
- 2026-07-12 guide-attribution audit: the four decision guides had attributed final CTAs, but several header product links dropped the source entirely, and the construction comparison incorrectly reused `size_guide`. Added a distinct privacy-safe `comparison_guide` server allowlist value and made every product-return link on each guide carry its truthful source (`size_guide`, `measure_guide`, `tie_guide`, or `comparison_guide`). TDD red failed on both the missing server value and untagged header links; green passed the focused contracts and full 28-test suite. Syntax checks and a production Docker build passed. No visit, order, payment, or revenue was created.
- Public guide-attribution QA passed on 2026-07-12 for commit `d9891c0`: validation run `29210262548`, Pages run `29210262568`, and GHCR run `29210262553` succeeded. All four Pages guides returned HTTP 200, exposed their distinct expected source, and contained no untagged `index.html#shop` link. Render was still live on old commit `b4ac194` and returned 404 for the comparison guide, so a manual latest-commit deploy advanced it to `d9891c0` and the dashboard reported live. The Render comparison guide then returned HTTP 200 with `comparison_guide`; readiness remained safely closed with `ready:false`, `checkoutEnabled:false`, two missing Stripe secrets, and no public price. This proves attribution deployment, not a referral, checkout, payment, or revenue.
- 2026-07-12 external quality baseline: DataForSEO Lighthouse 13.4.0 fetched the public homepage successfully and reported performance 0.96, accessibility 0.93, best practices 0.96, SEO 1.00, LCP about 1.4 seconds, and 1,614 KiB total transfer. Exact failures were four small guide-number labels at 4.21:1 contrast, a focusable close button inside the `aria-hidden` cart, and a missing root favicon causing a console 404. Added a local SVG favicon to every publication allowlist, made the closed cart inert with synchronized JS state, and changed the small labels to the higher-contrast primary color. TDD red covered the missing favicon and focus isolation; a test initially referenced the wrong fixture name, was corrected, and the full 30-test suite plus syntax and production Docker build passed. Post-deployment Lighthouse evidence is still required.
- Public accessibility QA passed on 2026-07-12 for commit `05a9b40`: validation run `29210454382`, Pages run `29210454404`, and GHCR run `29210454377` succeeded. Direct probes returned HTTP 200 for the homepage and local SVG favicon (`image/svg+xml`) and confirmed the inert closed-cart markup. A same-tool Lighthouse 13.4.0 rerun reported accessibility 1.00, best practices 1.00, SEO 1.00, and no failing contrast, hidden-focus, or console-error nodes. Performance varied from 0.96 to 0.94 with LCP about 1.6 seconds and 1,609 KiB transferred, so no speed improvement is claimed. These are lab-quality diagnostics, not field traffic, conversion, or revenue evidence.
- 2026-07-12 homepage payload pass: the Lighthouse request table proved that the production-pattern PNG alone transferred 1,380,932 bytes because the CSS hero loaded it, while the exact 254 KB digital mockup was already fetched for the product card. Reuse the mockup as the clipped hero background so the browser downloads one truthful product asset instead of both. The supplier pattern remains unchanged and available for production and guide banners. TDD red failed on the pattern-backed hero; green passed the new asset contract and full 31-test suite. Syntax and production Docker build passed. Public byte/LCP evidence is still required after deployment.
- Public homepage-payload QA passed on 2026-07-12 for commit `262e8cf`: validation run `29210558738`, Pages run `29210558730`, and GHCR run `29210558716` succeeded. A same-tool Lighthouse 13.4.0 rerun reported performance 1.00, accessibility 1.00, best practices 1.00, SEO 1.00, LCP about 0.6 seconds, and 266,984 bytes (261 KiB) transferred. The request table contained no production-pattern request and one 254,881-byte mockup request. Against the immediately prior 1,609 KiB / 1.6-second run, this is about 83.8% fewer transferred bytes and a 1.0-second lower lab LCP. It is lab performance evidence, not field traffic, conversion, or revenue.
- 2026-07-12 backend provenance pass: Render's official current documentation confirms `RENDER_GIT_COMMIT` is a default runtime variable containing the deploy commit SHA. Added `/api/version`, which returns only a validated lowercase 40-character SHA or null, plus `npm run verify:deployment -- <full-sha>` to fail deterministically on unknown, stale, or unreachable deployments. TDD red returned HTTP 404 for the absent endpoint; green passed the real-server contract, full 31-test suite, syntax checks, and production Docker build. Public deployment and exact-match evidence are still required.
- Public backend-provenance QA passed on 2026-07-12 for commit `3faa9d8`: validation run `29210651891`, Pages run `29210651896`, and GHCR run `29210651897` succeeded. Before the Render deploy, `npm run verify:deployment -- 3faa9d8f4c9a357442e39908bb64e6731f83f08c` failed on HTTP 404, correctly proving the live backend was stale despite green checks. A manual latest-commit deploy reached live on the exact SHA, after which the same command returned `Render deployment matches 3faa9d8.` Readiness remained `ready:false`, checkout disabled, two Stripe secrets short, and without a public price. This proves backend code freshness, not commerce readiness, payment, or revenue.
- 2026-07-12 keyword-demand pass: DataForSEO Google Ads US English data reports `dog bandana` 18,100 monthly searches, `pet bandana` 1,000, `cat bandana` 1,900, `dog bandana size chart` 720, `tie on dog bandana` 720, `over the collar dog bandana` 390, and `how to tie a dog bandana` 140. Its separate intent model labels `tie on dog bandana` transactional with probability 0.771, the strongest close product match supported by Pet Parade. Strengthened the existing comparison URL rather than creating a cannibalizing page: title/H1 now lead with `Tie-on dog bandana`, meta/OG copy remains factual, Article JSON-LD names the canonical, and the homepage internal anchor reflects the intent. TDD red failed on missing exact targeting/schema; green passed the full 31-test suite, syntax checks, and production Docker build. These are US market estimates, not international demand, ranking, impressions, clicks, or revenue.
- Public transactional-intent QA passed on 2026-07-12 for commit `2eccfd2`: validation run `29210837825`, Pages run `29210837765`, and GHCR run `29210837767` succeeded. The exact comparison URL returned HTTP 200 with the targeted title/H1 and canonical Article JSON-LD. DataForSEO's public on-page parser confirmed title `Tie-on dog bandana vs. over-collar styles`, the exact canonical, title-to-content consistency 1.00, CLS 0, and on-page score 97.44. IndexNow received all seven URLs with HTTP 200. This proves publication and on-page alignment only, not crawling, ranking, impressions, clicks, checkout, or revenue.
- 2026-07-12 commercial size-chart pass: the same DataForSEO US English snapshot reports `dog bandana size chart` at 720 monthly searches and commercial intent probability 0.656. Strengthened the already Google-indexed `bandana-size-guide.html` rather than creating a duplicate: title/H1/OG/Article headline now lead with the measured phrase and the homepage anchor matches it. The verified 44/54/64 cm table, non-universal-label warning, and S-for-small-pets limitation remain unchanged. TDD red failed on the generic title; green passed the full 32-test suite, syntax checks, and production Docker build. Public publication/on-page evidence is still required; volume does not prove ranking, impressions, clicks, or revenue.
- Public commercial size-chart QA passed on 2026-07-12 for commit `08829bd`: validation run `29210957880`, Pages run `29210957896`, and GHCR run `29210957911` succeeded. The exact indexed URL returned HTTP 200 with its new title/H1/Article headline, all three verified measurements, and the small-pets warning. DataForSEO's public parser confirmed the exact canonical, title-to-content consistency 1.00, description-to-content consistency 0.90, on-page score 97.44, and CLS 0. IndexNow received all seven URLs with HTTP 200. This proves publication/on-page alignment only, not recrawl, ranking, impressions, clicks, checkout, or revenue.
- 2026-07-12 homepage category-intent pass: DataForSEO's US English snapshot reports `pet bandana` at 1,000 monthly searches with primary transactional intent probability 0.476. The homepage already represents the sole product/category URL, but its creative H1 omitted the category. Updated title, H1, meta/OG/Twitter descriptions, WebSite schema, and lede to name a `pet bandana` with the factual original cat-and-dog print while retaining the pre-launch/no-orders notice and all unresolved launch gates. TDD red failed on the generic category wording; green passed the full 33-test suite, syntax checks, and production Docker build. Public on-page evidence is still required; a US estimate is not international traffic, ranking, clicks, checkout, or revenue.
- Public homepage category-intent QA passed on 2026-07-12 for commit `0cffa72`: validation run `29211049091`, Pages run `29211049062`, and GHCR run `29211049097` succeeded. The homepage returned HTTP 200 with the category title/H1/schema and explicit closed-order status. DataForSEO confirmed the exact canonical, title-to-content consistency 1.00, description-to-content consistency 0.952, on-page score 97.44, and CLS 0. Lighthouse 13.4.0 retained performance/accessibility/best-practices/SEO at 1.00 with lab LCP about 0.34 seconds and 267,014 bytes transferred. IndexNow received all seven URLs with HTTP 200. This proves publication and on-page quality only, not recrawl, ranking, impressions, clicks, checkout, or revenue.
- 2026-07-12 human launch-gate audit: readiness already required checkout enablement, automatic tax, HTTPS, Printful configuration, auto-confirm, and every technical variable, but it did not encode four human gates repeatedly promised in public copy. With all technical settings present, a regression reproduced `ready:true` despite no sample, supplier billing, customer-policy, or private-support approval. Added four required false-by-default variables and a public `launchApprovals` object; readiness now requires all four true. Red proved the bypass; green proves technical-only remains false and the fully approved configuration becomes true. The Blueprint pins every approval to `false`; focused contracts, full 35-test suite, syntax checks, and production Docker build passed. No approval was granted and checkout remains closed.
- Public human-gate QA passed on 2026-07-12 for commit `06100c4`: validation run `29211168957`, Pages run `29211168948`, and GHCR run `29211168954` succeeded. A manual latest-commit Render deploy reached live, and the exact deployment verifier matched full SHA `06100c4b4477024a6f6a4bbc67b88d7546b7fcfd`. Public readiness reports `ready:false`, checkout/tax/auto-confirm false, sample/supplier-billing/customer-policies/private-support all false, no public price, and six missing variables (the two Stripe secrets plus four not-yet-configured approvals). The count increase from two is intentional hardening, not lost configuration. No approval, payment, or revenue was created.
- 2026-07-12 cat-intent acquisition pass: DataForSEO's same US English snapshot reports `cat bandana` at 1,900 monthly searches, above the 1,000 measured for `pet bandana`. Added a distinct decision guide rather than a duplicate product page. It gives only defensible guidance: compare an already tolerated square, uses the verified 44/54/64 cm dimensions, states that S is not a cat-fit guarantee, requires supervision, and says the accessory is not a collar or restraint. The page returns with a dedicated `cat_guide` attribution and is included in Pages, Render, Docker, both sitemaps, IndexNow, the homepage library, canonical/social contracts, and container trigger paths. TDD red failed on the absent page; green passed the full 36-test suite, syntax checks, Dockerfile check, and production image build. Public deployment evidence is still required; search volume is not ranking, traffic, or revenue.
- Public cat-guide QA passed on 2026-07-12 for commit `f8aa996`: validation run `29211361639`, Pages run `29211361632`, and GHCR run `29211361624` all succeeded. The exact Pages and Render guide URLs returned HTTP 200 with the canonical page and `cat_guide` product attribution. IndexNow accepted all eight URLs with HTTP 200. Render did not auto-deploy after the green workflows and still reported `06100c4`; an authenticated manual latest-commit deploy advanced it, after which the deployment verifier matched full SHA `f8aa996033d2a2cb6ce64b9bc02786871d991aef`. Readiness remains deliberately closed with all four human approvals false and six missing settings. The live revenue verifier could not query Stripe because no protected key is locally available, and there is no durable paid-order evidence, so verified revenue remains USD 0.00.
- 2026-07-12 cat-guide discovery pass: Pinterest, X, and Reddit all exposed signed-out surfaces in authenticated Chrome, so no social profile was invented and no post was claimed. Google URL Inspection initially reported that `cat-bandana-guide.html` was unknown and not indexed. Its live test at 18:25 EDT then confirmed `Google has access to this URL` and `The page can be indexed`; Search Console subsequently confirmed `Indexing requested` and that the exact URL entered the priority crawl queue. Updated acquisition documentation from the stale four-URL count to the current eight and added truthful cat-guide social copy for a future authenticated channel. This proves fetchability and a crawl request only, not indexation, ranking, impressions, clicks, orders, or revenue.
- 2026-07-12 customer-policy draft pass: the transparency page disclosed policy gaps but did not provide one usable merchant policy. Official Printful guidance confirms 30-day claims after delivery for damaged/misprinted/defective goods, 30 days after the estimate for transit loss, no supplier-funded change-of-mind remedy, tracking after shipment, and possible scan delay. Official FTC guidance requires a reasonable basis for shipping promises and, on delay, the choice to consent or cancel for a full refund. Added `customer-policies.html` with separate US-pilot shipping, defect/loss remedy, made-to-order limitation, cancellation, and privacy sections; it explicitly keeps orders closed and policy approval pending. Added it to the homepage, transparency page, both sitemaps, IndexNow, Pages, Render, Docker, canonical/social contracts, and container triggers. TDD red failed on the absent page; green passed 37 tests, syntax checks, Dockerfile validation, and a production image build. Publication does not approve the policy gate because private support, exact delivery disclosure, data retention, and remedy operations are not yet proven.
- Public customer-policy QA passed on 2026-07-12 for commit `eb63392`: validation run `29211674227`, Pages run `29211674221`, and GHCR run `29211674217` all succeeded. The Pages policy returned HTTP 200 with its shipping policy and closed-order disclosure, and IndexNow accepted the expanded nine-URL set with HTTP 200. Render was still on `f8aa996`, so an authenticated manual latest-commit deployment advanced it; the exact verifier then matched full SHA `eb63392a1a719ddf3de89108a7635f1ea3809741`. The Render policy returned HTTP 200 with its refund/replacement policy while protected readiness still reported `customerPolicies:false`, `ready:false`, and six missing settings. This proves publication and a safely closed approval gate, not legal approval, customer support, checkout, payment, or revenue.
- 2026-07-12 supplier-billing enforcement pass: a targeted authenticated Printful audit showed no prior payments and a billing-method page asking to add a payment method; no card identifier or expiry was present, so supplier billing remains unapproved and no financial data was exposed. Code audit then found that human approvals controlled readiness/UI but not the server checkout operation: with complete technical variables and approvals absent, an allowed-origin POST reached Stripe and returned 400 instead of stopping at 503. TDD red reproduced that server bypass. `createCheckoutSession` now requires complete readiness before any Stripe call, while already-paid webhooks retain safe recovery but can auto-confirm/charge Printful only when both the auto-confirm flag and supplier-billing approval are true. Focused tests, all 38 tests, syntax checks, Dockerfile validation, and a production image build passed. No payment method, charge, Stripe session, order, or revenue was created.
- Public launch-enforcement QA passed on 2026-07-12 for commit `a9753a4`: validation run `29211878994`, Pages run `29211878989`, and GHCR run `29211878983` all succeeded. Render remained on `eb63392`, so an authenticated manual latest-commit deployment advanced it and the exact verifier matched full SHA `a9753a4759d836fceac93f262a1804e13ac7fc5f`. A public checkout POST carrying the exact allowed storefront Origin then stopped at HTTP 503 with `Checkout is not live yet.`, while readiness independently reported `supplierBilling:false` and `ready:false`. This proves the server blocks new payment creation before Stripe when human gates are closed; no payment or revenue was created.
- 2026-07-12 private-support routing pass: installed the authorized Gmail connector and verified the connected personal mailbox without publishing it. No existing PawSwipe label was present. Created the private `PawSwipe Support` label, sent a no-customer-data self-test through a private plus-address, found the exact test message, applied the label, and verified one unread labeled message. This proves internal delivery and organization only. Added `docs/SUPPORT_OPERATIONS.md` with privacy boundaries, six triage categories, response templates, and explicit external-intake/monitoring/reply/lookup/escalation approval evidence. Added a public-page regression that forbids the personal mailbox or Gmail domain. `PAWSWIPE_PRIVATE_SUPPORT_APPROVED` remains false because there is no privacy-safe public entry point or external end-to-end response test.
- Private-support operations QA passed on 2026-07-12 for commit `6ac62b0`: local 39-test and syntax suites passed, GitHub validation run `29212101194` succeeded, and Pages run `29212101115` succeeded. The personal mailbox and routing address remain absent from every indexable public page by executable contract. This is privacy and internal-operations evidence only, not a public support launch or approval.
- 2026-07-12 private-support approval pass: created and published `PawSwipe private support request` in Google Forms under the connected owner account. Anonymous HTTP returned 200 with the exact form title and no personal mailbox. The form discloses Google hosting and collects only help category, validated reply email, optional PawSwipe order reference, message, and required privacy confirmation; it warns against cards, passwords, one-time codes, government IDs, and unrelated data. Owner email notifications are enabled. A no-customer-data submission was recorded as the first response, produced a Gmail notification that was routed to `PawSwipe Support`, and received a verified acknowledgement at the test requester alias. Updated transparency and customer policies with the real private link and one-US-business-day initial-review target. All runbook evidence is now satisfied, so `PAWSWIPE_PRIVATE_SUPPORT_APPROVED` becomes the first true human gate; sample, supplier billing, and customer-policy approvals remain false.
- Public private-support QA passed on 2026-07-12 for commit `594e6b4`: validation run `29212431812`, Pages run `29212431826`, and GHCR run `29212431845` all succeeded. Pages and anonymous Google Forms probes returned HTTP 200 with the private support link/title and no personal mailbox. Render required a manual latest-commit deploy and matched full SHA `594e6b40ddbc33e36f15fb3930a94442bfd79344`. A manual Blueprint sync then required explicit approval and created all four approval variables from source: private support became true while sample, supplier billing, and customer policies remained false. Missing-variable count changed from six to two because the three false approvals now exist as explicit values; only the two Stripe secrets are absent. `ready:false` and a public allowed-Origin checkout HTTP 503 prove commerce remains closed. This is the first fully evidenced human gate, not a payment or sale.
- 2026-07-12 Stripe account-state audit: Gmail metadata proves the connected owner received Stripe email verification in 2021, an unknown-device notice in 2021, Google-sign-in enablement in April 2026, and a current product notification in June 2026. Google OAuth reached the same existing account, but Stripe again required the six-digit SMS factor for the phone ending in 3905. `Use another method` exposed only that SMS factor or account recovery. No code was requested, read, relayed, stored, or bypassed, and recovery was not started. Added `docs/PAYMENT_OPERATIONS.md` with the owner-completed sandbox-to-live sequence and evidence standard. A TDD regression also exposed that `.env.example` omitted every human approval; the template now defaults all four false. This does not provide dashboard access, Stripe secrets, payment, or revenue.
- Stripe activation-handoff QA passed on 2026-07-12 for commit `08180e1`: all 41 local tests and syntax checks passed, validation run `29212660909` succeeded, and Pages run `29212660945` succeeded. The public environment template now cannot omit human approvals without failing CI. No runtime configuration, Stripe access, payment, or revenue changed.
- 2026-07-12 privacy-data-map pass: audited the exact Google Forms, Stripe Checkout, Render, and Printful fields against current code and official retention sources. The public policy now identifies hosting request data, the five support fields, Stripe billing/shipping/contact/payment/tax/fraud data and four limited metadata fields, plus every Printful recipient and order field. Routine support records use a 24-month schedule; merchant transaction and fulfillment records follow the CRA six-year rule; Render Hobby logs are available for seven days; Stripe and Printful purpose-based retention remains accurately attributed to those providers. A new executable contract ties the disclosures to the current checkout and fulfillment source. All 42 tests, syntax checks, and diff checks passed. `customerPolicies` deliberately remains false because a supported pre-payment delivery estimate and end-to-end remedy execution are still not proven. Publication/deployment QA remains required.
- Public privacy-data-map QA passed on 2026-07-12 for commit `872fdb9`: validation run `29212809917`, Pages run `29212809908`, and GHCR run `29212809888` all succeeded. GitHub Pages returned HTTP 200 with the six-year transaction schedule, 24-month routine-support schedule, Stripe Checkout fields, and Printful fulfillment fields. Render was stale on `594e6b4`, so an authenticated manual latest-commit deploy advanced it and the exact deployment verifier matched `872fdb9c9c49cce8952bc885b9729a05c4a8e43e`. The Render policy returned HTTP 200 with the retention disclosure, while a checkout POST from the exact allowed Origin returned HTTP 503 and readiness stayed `ready:false`, `customerPolicies:false`, `privateSupport:true`, with only the two Stripe secrets absent. This proves publication and safe gate separation, not checkout, payment, or revenue.
- 2026-07-12 US delivery-evidence pass: changed Printful's authenticated global selling region from Canada to the US and reloaded the exact All-Over Print Bandana product. The catalogue displayed an approximate 9-11-business-day US delivery estimate and the same range for S, M, and L. Added required min/max delivery variables, ordering validation, and Stripe Checkout's structured `business_day` shipping estimate so a supported range will appear before payment. The public policy now dates and qualifies the evidence, while `docs/SHIPPING_OPERATIONS.md` requires weekly rechecks, live closure on drift, tracking capture, delay consent/refund handling, a visible Stripe test-session check, and a safe remedy drill. `customerPolicies` remains false because payload coverage is not visual Stripe evidence and the payment-to-fulfillment remedy drill is not yet possible. All 43 tests, syntax checks, diff checks, production Docker build, and a real container readiness start passed. Publication/deployment QA remains required; no order or revenue was created.
- Public US delivery-evidence QA passed on 2026-07-12 for commit `a5ca3f0`: validation run `29213119111`, Pages run `29213119173`, and GHCR run `29213119118` all succeeded. Manual Render deployment reached exact SHA `a5ca3f051d8783eede4e3a1f5a25d6d32113df51`; Blueprint sync explicitly created only the 9 and 11 business-day variables. Pages and Render customer-policy URLs returned HTTP 200 with the dated 9-11-business-day range. Readiness remained `ready:false`, `customerPolicies:false`, `privateSupport:true`, with only the two Stripe secrets missing, and an exact allowed-Origin checkout POST returned HTTP 503. This proves current supplier evidence, public disclosure, configuration, and launch-lock integrity—not a rendered Stripe test session, actual delivery, payment, or revenue.
- 2026-07-12 transactional-guide discovery pass: Search Console on the exact PawSwipe property still showed performance and aggregate indexation processing, with no query/page data, impressions, or clicks. Both submitted sitemaps still reported unknown type, fetch failure, and zero discovered pages; no resubmission was attempted. URL Inspection showed the already-published `tie-on-vs-over-collar-dog-bandana.html`—the strongest measured close intent at 720 US monthly searches and transactional probability 0.771—as unknown and not indexed, with no referring source detected. A live test at 19:20 EDT confirmed Google access and indexability, then Search Console confirmed the URL entered its priority crawl queue. This proves fetchability and a crawl request only, not indexation, ranking, traffic, orders, or revenue.
- 2026-07-12 launch-notification intake pass: created and published the Google-hosted `PawSwipe launch notification` form at a public anonymous URL. It collects only a response-validated email and a required checkbox consenting to one launch email for eligible US ordering. The form and storefront prohibit newsletters, promotions, tracking pixels, and unrelated personal data; the deletion schedule is within 30 days after sending, earlier on verified request, or by 2027-07-12 if ordering has not opened and consent was not renewed. Anonymous HTTP returned 200 with the title and both fields without exposing the owner mailbox. A synthetic `example.com` submission passed validation and consent, appeared as one response, and was permanently deleted; the queue returned to zero while owner response notifications remained enabled. Added the CTA, policy, transparency disclosure, privacy data-map row, operating runbook, and executable contract. All 44 tests, syntax checks, diff checks, production Docker build, and a real container homepage probe passed. This proves a privacy-bounded interest intake, not a real subscriber, email delivery, order, payment, or revenue. Publication/deployment QA remains required.
- Public launch-notification QA passed on 2026-07-12 for commit `89ec958`: Pages run `29213570797` and GHCR run `29213570807` succeeded. Validation run `29213570812` initially failed only because Docker Hub returned HTTP 502 while resolving the pinned Node base image; its identical failed-job rerun succeeded, with no code or image change. Manual Render deployment reached exact SHA `89ec958e146f247f1833b4ac23b1558fca5896a8`. Pages and Render homepages returned HTTP 200 with the one-launch-email CTA. The anonymous Google Form returned HTTP 200 with required consent and no owner mailbox. Readiness remained false and an allowed-Origin checkout POST returned HTTP 503. The private queue remains at zero after synthetic-test deletion. This proves publication and safe intake, not a real subscriber, launch-email delivery, checkout, payment, or revenue.
- 2026-07-12 synthetic order-remedy drill: exported the production Printful external-ID derivation and added a private, read-only classifier for delay, transit loss, defects, and cancellation. It stops unmatched or unpaid cases, checks the published 30-day windows, and returns only a short session fingerprint, hashed Printful reference, issue kind, booleans, and a manual next action; it cannot refund, cancel, replace, message, or mutate providers. Five focused tests include fake PII and prove it never appears in output. A complete CLI drill on `test/fixtures/remedy-delay.json` correlated the exact derived reference and returned `offer_delay_consent_or_full_refund`; a separate output scan rejected raw session ID, fake email, phone, and address leakage. All 49 tests, syntax checks, and diff checks passed. This proves synthetic correlation/redaction/routing only. `customerPolicies` remains false until a provider-backed Stripe/Printful test and visible Stripe delivery-range check pass; no customer outcome, payment, or revenue was created.
- Order-remedy CI QA passed on 2026-07-12 for commit `ccb55c8`: validation run `29213744847`, Pages run `29213744852`, and GHCR run `29213744877` all succeeded. The private CLI is intentionally not exposed as a public endpoint and no Render deployment was claimed. This proves reproducible repository/build coverage only, not provider access or a real remedy.
- 2026-07-12 Bing ownership-proof pass: the exact PawSwipe property became navigable in the existing Bing Webmaster Tools session, but Sitemaps still returned `User is unauthorized to access the site`; homepage navigation was not ownership. Reopened manual add for the exact URL and refused the Google Search Console import path because it requests new cross-provider access. Bing issued a property-specific public `BingSiteAuth.xml`; added it to Pages, Render, Docker, and container-trigger allowlists plus an executable XML/MIME contract. The first test expected `application/xml` but the server correctly includes `charset=utf-8`; fixed the assertion to the exact real header. All 50 tests and syntax checks passed, the production image built, and a real container returned HTTP 200 `application/xml; charset=utf-8` with the `<users>` document. Public deployment and Bing verification remain required; no sitemap/indexing/traffic evidence is claimed yet.
- Public Bing ownership QA passed on 2026-07-12 for commit `cf82015`: validation run `29213900116`, Pages run `29213900118`, and GHCR run `29213900134` succeeded. Pages served `BingSiteAuth.xml` with HTTP 200 `application/xml`; manual Render deployment reached exact SHA `cf82015469d2b58ef4b964e09608bc94c874ab45` and served the same token with HTTP 200 `application/xml; charset=utf-8`. Bing Verify then removed the unauthorized state and exposed the real sitemap dashboard for the exact PawSwipe property. Submitted `sitemap.xml`; Bing reported one known sitemap, zero errors/warnings, zero discovered URLs, and `Processing`. Direct URL Submission recorded all nine current public URLs at 19:46 EDT, reducing quota from 100 to 91. These are ownership and queue-receipt facts only, not crawl, indexation, performance, AI citation, click, order, or revenue evidence.
- 2026-07-12 Bing URL-inspection pass: attempted a nine-page Site Scan on the verified property, but Bing showed `Quota left: 0 pages` with disabled controls, so no scan was claimed. Homepage URL Inspection reported `Indexed successfully`, eligible to appear on Bing, no SEO/GEO issues, and one markup type. The strongest measured comparison guide reported `Discovered but not crawled`, discovered July 12, and unable to appear; its priority action returned `Indexing requested`. No source change was justified because the only page Bing evaluated had no issue and the guide's gap is crawl state, not a reported markup or SEO defect. This proves one indexed homepage and one crawl request, not ranking, impressions, clicks, orders, or revenue.
- 2026-07-12 guide-conversion pass: Google Search Console performance and aggregate page-indexing reports were rechecked on the exact PawSwipe property. Both still said processing would take about one day; performance showed a three-hour-fresh update but disabled query/page tabs and no data, so no query claim or data-driven content change was made. Added the already-verified one-message launch-notification CTA to all five commercial decision guides, immediately beside the closed-order disclosure and the exact `No newsletter or promotions` limit. A new contract fails if any guide loses the form link or consent copy. All 51 tests, syntax checks, diff checks, and local rendered DOM QA passed. This creates a privacy-bounded conversion opportunity for future organic visitors; it does not prove a subscriber, order, payment, or revenue. Public deployment QA remains required.
- Public guide-conversion QA passed for commit `8100de7`: validation run `29214253376`, Pages run `29214253412`, and GHCR run `29214253385` all succeeded. Each of the five Pages guide URLs returned HTTP 200 with the launch-email CTA, the no-newsletter/no-promotions limit, and an explicit closed-order statement. Render's on-commit behavior again did not advance automatically, so a manual latest-commit deploy reached exact SHA `8100de7907b8d749a5214537c065c891d09f8f44`; the comparison guide returned HTTP 200 with the CTA and limit while an exact allowed-Origin checkout request remained HTTP 503. This proves public conversion-path availability and launch-lock preservation, not a subscriber, payment, order, or revenue.
- 2026-07-12 authenticated Stripe setup pass: the owner completed Stripe authentication directly. The `SiteQC` dashboard is accessible, CAD-based, and has prior live payout evidence, but its setup guide still shows email, business-information, and Stripe-profile confirmation work, so PawSwipe live activation remains unapproved. Stripe's visually masked API-key page exposed full test values through its accessibility representation; every exposed standard and restricted key was immediately renewed with zero delay and never deployed. A replacement test standard secret was captured without output, transferred directly to protected Render, then cleared. Created active test webhook destination `we_1TsXTzCuBApzNOkmrwUJgjc4`, named `PawSwipe Render test checkout`, for the exact Render endpoint and only the two Checkout success events; its signing secret was also transferred directly and cleared. Render readiness reached missing configuration count zero while all commerce gates remained closed, but exposed a Blueprint conflict: `render.yaml` kept forcing `STRIPE_CHECKOUT_MODE=live` despite test credentials. Changed both Blueprint and public template to default `test` and added a regression. Blueprint sync, test-event delivery, and public post-deploy verification remain required. No real or test payment was created and verified gross revenue remains zero.
- Stripe test-event diagnosis: Blueprint sync of commit `4ee7554` was approved and the protected readiness endpoint then reported `checkoutMode:test`, zero missing variables, and `ready:false`. Stripe Shell successfully triggered `checkout.session.completed`, but delivery returned HTTP 500. Sanitized Render logs identified `Checkout is not live yet.`: `liveCheckoutConfig()` incorrectly required live checkout and automatic tax before authenticating any signed webhook, contradicting the intended paid-event recovery path. Added a signed-HMAC regression proving a non-payment test event returns 200 while checkout and tax remain false, and split configuration so only new-session creation requires those two gates. All 53 tests and syntax checks pass locally. Deployment and provider redelivery remain required; no payment or revenue was created.
- Public signed-webhook QA passed for commit `8cecde3`: validation run `29215323611`, Pages run `29215323610`, and GHCR run `29215323589` succeeded. Manual Render deployment reached exact SHA `8cecde33077257691450335dc609f34849fc0ad3`; readiness remained `checkoutMode:test`, zero missing variables, and `ready:false`. Early provider redeliveries still returned 500 because the private DOM extraction had appended the adjacent French status `Aucun` to the 107-character test secret. Renewed the exposed key, extracted only the exact copy-control descendant, validated the 107-character replacement directly against Stripe, replaced and privately revealed the protected Render value, and independently received HTTP 200 from the same harmless Stripe API request. A fresh Stripe Shell `checkout.session.completed` trigger then appeared as `200 OK` in the exact webhook destination. The fixture has no PawSwipe metadata, so `shouldFulfillStripeSession` stopped before Printful; no draft, charge, payment, or revenue was created. The last exposed generic restricted key was also renewed with zero delay.
- 2026-07-12 provider-backed PawSwipe checkout drill: created a fresh test Checkout Session directly through Stripe with production-equivalent size-M parameters. The first API request was rejected before session creation because current Stripe requires `shipping_rate_data[type]=fixed_amount`; added the missing server field and an executable regression. The corrected session visibly displayed USD 24.99 product, USD 4.49 standard tracked shipping, 9–11 business days, and USD 29.48 total. Using Stripe's official test card plus synthetic `example.com`/US contact data produced a complete `livemode:false`, `payment_status:paid` session. Authoritative API retrieval confirmed USD currency, USD 24.99 product subtotal, USD 4.49 shipping, PawSwipe store metadata, `PP-BANDANA-M`, size M, direct attribution, one line item, and collected shipping. The exact webhook destination recorded another Checkout completion delivery. A private Printful token validation returned 200, and the Orders API found exactly one matching external-ID order in `draft` state with STANDARD shipping, one item, costs, and recipient; no confirmation or supplier charge occurred. This closes the visible-delivery and provider-backed payment-to-draft evidence required for `PAWSWIPE_CUSTOMER_POLICIES_APPROVED=true`. Sample and supplier-billing approvals remain false, as do automatic tax and live checkout. Test money is excluded from revenue; verified gross remains USD 0.00.
- Deployment proof for customer-policy approval: commit `e0ef6c193ca3575137cc9fbf4572e8b66972251b` passed validation run `29216382760`, Pages run `29216382801`, and GHCR run `29216382809`. Render Blueprint sync explicitly approved the customer-policy variable and deployed that exact full SHA. Production readiness then reported `customerPolicies:true`, `privateSupport:true`, `sample:false`, `supplierBilling:false`, test mode, tax false, auto-confirm false, checkout false, ready false, and zero missing configuration; an allowed-origin checkout request returned HTTP 503. GitHub Pages served the updated public gate wording. Revenue remains USD 0.00/100.00.
- 2026-07-12 post-drill merchant-boundary audit: Stripe is the pre-existing CAD `SiteQC` account with a prior payout and an incomplete six-step profile guide. Its guide is prefilled with `siteqc.ca` and requests a business activity description, so no PawSwipe name, website, legal fact, live key, or tax state was invented or submitted. Printful's authenticated PawSwipe billing page has no account or store payment method; CAD, USD, and every other visible wallet are zero with automatic funding disabled. This is authoritative evidence that sample purchasing and supplier charging remain unavailable. Updated stale public returns text and operating records to reflect the already-approved customer-policy/support gates without weakening the remaining sample, billing, tax, destination, or checkout locks. Revenue remains USD 0.00/100.00.
- Merchant-boundary publication QA passed for commit `aace2b89b2898ba67d252b150dbb51f951c7a00c`: validation run `29216754554`, Pages run `29216754577`, and GHCR run `29216754536` all succeeded. The public transparency page serves the corrected provider-backed policy status. This publishes accurate gate state only; it does not add a Printful payment method, wallet funds, sample, Stripe live configuration, order, or revenue.
- 2026-07-12 search-signal and shareability pass: Google Search Console performance still reports processing and no query/page rows; Bing is also processing, while its 3-month AI Performance report shows zero citations and zero cited pages. No data-driven ranking or AI claim was made. Extended the existing no-SDK/no-pixel direct Pinterest, X, and email share pattern from the size guide to the reference-measurement and highest-volume cat guide, with executable coverage. This creates visitor-controlled distribution capability only; it is not a post, impression, click, subscriber, order, or revenue. Verified gross remains USD 0.00/100.00.
- Shareability publication QA passed for commit `6c69e1a6d79cd2b7f268447ae5479a509c42f943`: validation run `29216979975`, Pages run `29216979992`, and GHCR run `29216980014` all succeeded. Both newly updated public guides returned HTTP 200 and contained their Pinterest, X, and email composer URLs. This proves deployed visitor-controlled share capability only, not use or traffic.
- 2026-07-12 complete guide-share pass: extended the same privacy-safe Pinterest, X, and email composer pattern to the tying and tie-on-versus-over-collar guides. All five commercial decision guides now have topic-specific share copy and exact canonical guide URLs, without loading third-party SDKs, pixels, or cookies. Expanded the executable contract and durable operating rule accordingly. This is distribution capability only; it is not a published social post, impression, click, order, or revenue. Verified gross remains USD 0.00/100.00.
- Complete guide-share publication QA passed for commit `fb69997b6456160d147525d71076b5e375538bda`: validation run `29217094704`, Pages run `29217094723`, and GHCR run `29217094699` all succeeded. The two final public guides returned HTTP 200 with Pinterest, X, and email composers, and IndexNow accepted all nine current public URLs with HTTP 200. This proves publication and discovery notification only, not recrawl, sharing, traffic, or revenue.
- 2026-07-12 Atom discovery pass: added `feed.xml` with a canonical self link and six factual entries covering the storefront plus all five commercial guides. Added HTML autodiscovery to those six pages and wired the feed through Pages, Render, Docker, and IndexNow. A contract checks Atom namespace/self metadata, exact entry count and IDs, every HTML link, and every publication allowlist. This creates a standard syndication surface for English readers and aggregators; it is not a feed subscriber, crawl, citation, visit, order, or revenue. Verified gross remains USD 0.00/100.00.
- Atom publication QA passed for commit `9267ec695f9e68afe882a4313fea3002e3c7d50e`: validation run `29217200150`, GHCR run `29217200154`, and Pages run `29217200158` all succeeded. Pages served `feed.xml` as HTTP 200 `application/xml` with exactly six entries; the homepage exposed the exact Atom autodiscovery link, and IndexNow accepted all ten public discovery URLs with HTTP 200. The first homepage probe accidentally assigned to PowerShell's case-insensitive reserved `$HOME`, emitted a false value, and then fell through because the later IndexNow command succeeded. Discarded that observation, reran with `$homepageResponse`, obtained `AUTODISCOVERY=True`, and strengthened the project skill to make multi-step probes stop on any PowerShell error.
- 2026-07-12 visible feed-conversion pass: privately checked the launch-notification response surface without reading or exposing any address; there was no real conversion signal to act on. Added a visible email-free Atom subscription link beside the homepage launch CTA and in every commercial guide footer, while keeping the machine autodiscovery links. An executable contract requires all six visible routes. Response data and counts remain outside Git and memory; the durable fact is only that no content or campaign decision could be based on a real subscriber. This is a conversion opportunity, not a subscriber, visit, order, or revenue.
- Visible feed-route publication QA passed for commit `05b0f9b419b4ac9bda79248be5a74bd1a9f40efe`: GHCR run `29217310183`, validation run `29217310206`, and Pages run `29217310173` all succeeded. Direct HTTP probes found the email-free feed CTA on the homepage and all five commercial guides. This proves six deployed subscription routes only, not a feed subscriber or traffic.
- 2026-07-12 GitHub community pass: the repository had administrator access but no description, homepage, or Discussions. Set the factual English PawSwipe description, exact Pages homepage, and enabled Discussions. Published announcement discussion `#4`, `Welcome to the PawSwipe Pet Parade preview`, preserving the closed-order state, inviting guide/preview feedback, and prohibiting personal, address, payment, order, credential, identification, or private-support data. Anonymous HTTP returned 200 with the exact title and public-data warning. A post-publication URL-set audit caught that the initial body linked only four of five decision guides; updated the authoritative discussion and verified all five exact guide paths. Added the route to transparency with the GitHub-login/public-posting disclosure and private-support separation. This is the first visible published community surface, not a reader, reply, visit, order, or revenue. Verified gross remains USD 0.00/100.00.
- GitHub community publication QA passed for commit `8f67c52767b8f7971d0c416f679225cbd8c8369e`: validation run `29217431076`, GHCR run `29217431094`, and Pages run `29217431083` all succeeded. Anonymous HTTP verified the public Discussion route and its fifth guide link, while the deployed transparency page contained the exact community URL plus the GitHub-login/public-posting disclosure. This proves a reachable public community surface only, not readership or engagement.
- 2026-07-12 community conversion-loop pass: updated the authoritative GitHub announcement's direct storefront link to `?utm_source=github`, an already-allowlisted privacy-safe label. Added a visible homepage guide-library CTA back to Discussion `#4`, including GitHub-login and prohibited-data boundaries plus a transparency link. No username, comment, referrer, or full source URL enters attribution. This creates a measurable future community-to-paid-session path without visitor tracking; it is not a click, payment, order, or revenue. Verified gross remains USD 0.00/100.00.
- Community-loop publication QA passed for commit `4f5c4fae1b3fee76a6c35596e2e20061a141ac2e`: GHCR run `29217512049`, Pages run `29217512069`, and validation run `29217512048` all succeeded. Anonymous HTTP found the homepage community CTA and Discussion URL, while the Discussion body contained the `utm_source=github` storefront return. This proves the deployed two-way navigation and attribution contract only, not a click or sale.
- 2026-07-12 repository-discovery pass: GitHub's authoritative 14-day traffic endpoints returned no views, unique views, clones, unique clones, referrers, or popular paths, so no acquisition claim was possible. Added six factual repository topics (`pet-bandana`, `pet-accessories`, `print-on-demand`, `dropshipping`, `stripe-checkout`, `printful`). Reworked the README landing section for English pet visitors with the deployed digital mockup and explicit uninspected-sample/closed-order warning, `github`-attributed storefront link, all five decision guides, email-free Atom feed, and public Discussion/privacy boundary. Added an executable landing-page contract. This creates repository discoverability and navigation only, not a view, click, order, or revenue. Verified gross remains USD 0.00/100.00.
- Repository-discovery publication QA passed for commit `ec2cea57b48385c82abccac84bf2b1e2f700410a`: validation run `29217596965` and Pages run `29217596947` succeeded. No GHCR run was expected because the commit touched README/docs/tests only and the container workflow is path-filtered. Authoritative GitHub state showed the exact Pages homepage and all six topics; raw public README contained `github` attribution, all five guides, and Discussion `#4`. Inspecting the path filter exposed a separate omission: `feed.xml` was copied into Docker but absent from the container workflow trigger, so a feed-only change could leave the image stale. Added the missing trigger and a regression, and refined the skill to derive expected workflows from actual filters.
- Feed-trigger QA passed for commit `bb62741f2d6c8ebc0df747e2b269be11bfb9fdf8`: Pages run `29217852394`, GHCR run `29217852397`, and validation run `29217852368` all succeeded. The workflow-file change deliberately triggered all three surfaces, proving the container path-filter regression is active.
- 2026-07-12 Product rich-result correction: commit `e0fa381` published a Schema.org-valid Product entity using only factual optional fields and deliberately omitted offers/reviews/ratings. Its Pages, validation, and GHCR workflows passed, and a direct JSON parse passed, but those checks did not cover Google's eligibility contract. Search Console first confirmed the homepage is indexed, then its live URL test found one critical invalid Product item: `offers`, `review`, or `aggregateRating` is required. None can be truthfully provided while checkout and customer evidence are closed. Removed the Product entity instead of inventing evidence, changed the regression to forbid it pre-launch, and strengthened the project rule to require Google's live verdict before recrawl. The page remained accessible and indexable; no indexing request was sent while the defect existed. Verified gross remains USD 0.00/100.00.
- Product correction publication and recrawl QA passed for commit `fdf0ca8f377b51555045e26af263024505c5a3b0`: GHCR run `29218105067`, Pages run `29218105027`, and validation run `29218105081` all succeeded; direct public HTML confirmed the Product entity absent. A fresh Google live URL test at 21:47 EDT then changed from the prior warning to `Google has access to this URL`, `The page can be indexed`, with all relevant optimizations and no Product enhancement item. Only after that clean verdict, Search Console confirmed `Indexing requested` and priority crawl queue receipt. This proves defect removal, live indexability, and a recrawl request—not recrawl completion, ranking, impression, click, order, or revenue.
- 2026-07-12 material-intent acquisition pass: a current DataForSEO US English snapshot found `dog bandana material` at 720 monthly searches with transactional intent; `cat bandana` remained strongest at 2,400 and existing size/tie/construction pages already cover their returned terms. Created the sole canonical `dog-bandana-material-guide.html` target. It compares Printful product 655's official US/Mexico 100% recycled-polyester 110 g/m² specification with EU/Latvia 65% recycled polyester/35% polyester 100 g/m², plus single-sided print, double-folded edges, care-label reliance, microfibre release, and supervised accessory limits. It explicitly rejects comfort, durability, quality, or fit inference before sample inspection. Added `material_guide` attribution, homepage discovery, share intents, launch/feed conversion, Atom entry, README, both sitemaps, IndexNow, Pages, Render, Docker, container trigger, canonical/social contracts, and source-label coverage. This is measured intent coverage, not ranking, traffic, order, or revenue. Verified gross remains USD 0.00/100.00.
- Material-guide publication QA passed for commit `c78081fac0f25b778a3f39e41736455d9b18219a`: GHCR run `29218430527`, validation run `29218430520`, and Pages run `29218430526` all succeeded. The local production image served the exact route on its configured port with HTTP 200. Pages served the guide, homepage link, factual material values, canonical URL, and seven-entry Atom feed; IndexNow accepted all 11 discovery URLs with HTTP 200. GitHub Discussion `#4` now contains all six exact guide links. Render had not yet advanced to the new route during the post-push polling window and returned `Not found`; do not claim that surface deployed until a later exact HTTP 200. This proves public Pages publication and discovery submission, not ranking, traffic, engagement, order, or revenue.
- 2026-07-12 distribution follow-up: Render's authoritative dashboard showed the service still live at old commit `e0ef6c1`, explaining the material-guide 404 despite green GitHub workflows. Manually deployed latest main; build logs explicitly copied `dog-bandana-material-guide.html`, and deployment `dep-d9a4dqt7vvec738iilkg` reached `Your service is live`. Direct Render HTTP then returned 200 with the material facts; readiness remained test mode, zero missing configuration, checkout/tax/sample/supplier-billing false, customer-policy/private-support true, and ready false. Stripe's authenticated live Transactions surface showed no PawSwipe payment; only a pre-existing successful CAD SiteQC invoice was visible, so it is excluded and verified PawSwipe gross remains USD 0.00. GitHub's authoritative 14-day traffic APIs still returned zero views, clones, referrers, and paths. Created public Q&A Discussion `#5`, `What dog bandana material facts do you compare?`, with sourced regional specifications, non-claim boundaries, the exact guide, `github`-attributed storefront return, closed orders, and prohibited-data warning. Added the reciprocal guide route and durable community rule. This is an acquisition surface, not a view, reply, order, or revenue.
- Material community-loop publication QA passed for commit `8db50bf17db0e742991792edbaf338810c5edbe1`: validation run `29218756378`, Pages run `29218756379`, and GHCR run `29218756403` all succeeded. Anonymous HTTP returned 200 for both the exact guide and Discussion `#5`; the guide contained the public-Q&A/privacy disclosure and discussion link, while the discussion contained its exact title and guide backlink. Render still serves the material guide from the prior manual deployment, but the reciprocal Q&A link from this commit has not been manually promoted there; do not claim that latest Render HTML until directly verified. This proves the Pages/community loop, not a view, reply, click, order, or revenue.
- 2026-07-12 sample-path and search follow-up: Google Search Console still reported property processing, disabled query/page tabs, and no data, so no speculative SEO edit was made. Printful's authenticated US view quoted C$14.50 base, shipping from C$5.59, and 9-11 days. File Library still held exact pattern file `1014307040`, but the template-only list showed no API template card and direct template `104922382` redirected to that empty list. A fresh application of the exact file defaulted to `Okay / 46 DPI`, materially below the already-evidenced regular-repeat 30%, `Good / 152 DPI` template; the unsaved draft was discarded instead of overwriting supplier state. The project rule now requires preserving provider template/sync/variant evidence when a dashboard omits an API-created template. Render was manually promoted to latest main and its deploy log reached `Your service is live`; direct public QA remains required. No sample was ordered, no payment method was added, no supplier charge occurred, and verified gross remains USD 0.00/100.00.
- 2026-07-12 published-product/sample-draft correction: the store-specific Printful `Publié` tab did contain PawSwipe sync product `445876313` even though the template-only tab was empty. It exposed S/M/L references `6a53ef4031cc29`, `6a53ef4031cc68`, and `6a53ef4031cc81`. The exact M sample-order line editor proved the production configuration is regular repeat, 30% scale, `Good / 152 DPI`, and **white stitching**; prior black-stitching notes were corrected rather than forcing provider state to match memory. Printful auto-saved a one-item sample-order draft with a 20% sample discount: C$14.50 original, C$2.90 discount, C$11.60 subtotal before shipping and tax. The draft is parked at the address step; no address was invented or exposed, no method was added, no checkout/payment was submitted, and sample/billing approvals remain false. This is a materially shorter path to physical validation, not an order, charge, shipment, inspected sample, or revenue.
- 2026-07-12 Stripe Tax classification audit: the authenticated SiteQC Tax overview said Tax was active for one pre-existing Payment Link and no current location required attention, but the territories view said initial transaction data was still updating and exposed no verified PawSwipe registration. More importantly, account Tax settings default uncategorized products to personal-use SaaS. PawSwipe's inline physical bandana price had no `tax_code`, so enabling automatic tax could have inherited that unrelated classification. TDD red failed on the absent configuration and payload contract. Added required, format-validated `PAWSWIPE_PRODUCT_TAX_CODE`, configured `txcd_99999999` (General - Tangible Goods) for the physical product, and send it as `line_items[0][price_data][product_data][tax_code]`. Automatic tax, live checkout, sample, and supplier billing stay false. This corrects product classification plumbing; it does not prove nexus, registration, filing, remittance, live activation, payment, or revenue.
- Tax-classification deployment QA passed for commit `f73e992a56e1b098723738f9d2a2c6f65b249845`: validation run `29219585750`, GHCR run `29219585733`, and Pages run `29219585737` all succeeded. Render Blueprint sync required and received explicit approval to create `PAWSWIPE_PRODUCT_TAX_CODE`; the resulting deployment exposed the exact expected full SHA through `/api/version`. Public readiness remained safely closed with `ready:false`, `stripeTaxEnabled:false`, and zero missing configuration. This proves code/config deployment and prevention of SaaS-default inheritance, not a verified tax registration, calculation, filing, payment, order, or revenue.
- 2026-07-12 tax/merchant gate hardening: readiness modeled four human approvals but still allowed full readiness after those four became true, automatic tax was enabled, and any live Stripe key existed—even without a verified tax registration or a PawSwipe-owned merchant profile. TDD red proved the four-approved environment still became ready and that Blueprint/public templates lacked both gates. Added required `PAWSWIPE_TAX_APPROVED` and `PAWSWIPE_MERCHANT_ACCOUNT_APPROVED`, exposed only their booleans in `launchApprovals`, defaulted both false, and required all six human approvals. Public launch copy now names tax registrations and merchant-account readiness. This closes a launch bypass; neither gate is approved and verified gross remains USD 0.00/100.00.
- Tax/merchant-gate deployment QA passed for commit `3eaa19d573f9aced3088202fee4665132ea95475`: validation run `29219754359`, GHCR run `29219754360`, and Pages run `29219754337` all succeeded. Blueprint sync explicitly created both new false variables. Render `/api/version` matched the exact SHA; readiness returned zero missing settings and six approvals with only customer policies/private support true, while an allowed-origin checkout request returned HTTP 503. Pages visibly named both tax-registration and merchant-account gates. This proves the bypass is closed and disclosed, not approval, payment, order, or revenue.
- 2026-07-12 agent-discovery pass: current primary llmstxt.org material describes `/llms.txt` as a proposal for a concise Markdown content map, while current Chrome/Lighthouse documentation recognizes it for agentic browsing; neither source makes it a crawler permission or ranking guarantee. Added one subpath-correct `llms.txt` with the canonical storefront root, all indexable factual pages, Atom feed, two public discussions, and explicit closed-order/digital-preview/non-claim boundaries. The first contract failed because it expected `/index.html` instead of the canonical root and exposed that the care guide was omitted; corrected both before publication. Wired the artifact through Pages, Render, Docker, both sitemaps, IndexNow, container triggers, README, and executable coverage. This is proposed discovery capability, not proof of parsing, citation, referral, traffic, order, or revenue.
- Agent-map publication QA passed for commit `70b042e`: validation run `29219963831`, GHCR run `29219963830`, and Pages run `29219963858` all succeeded. Pages served `llms.txt` as HTTP 200 `text/plain` with the canonical root, care guide, and closed-order statement; IndexNow accepted all 12 discovery URLs. Render did not auto-deploy, so a manual latest-commit deployment reached live; `/api/version` matched the full commit, `llms.txt` returned HTTP 200 `text/plain` with the same closed-order boundary, and readiness remained false with zero missing configuration. This proves publication only, not parser adoption, crawl, citation, traffic, order, or revenue.
- 2026-07-12 material-guide indexation follow-up: public `site:` searches returned no PawSwipe brand or size-guide result, while Search Console aggregate performance/indexation reports were still processing. URL Inspection specifically reported `dog-bandana-material-guide.html` as unknown to Google with no referring sitemap or page. A fresh live test then proved `Google has access to this URL` and `The page can be indexed`; only after that clean verdict, Search Console confirmed `Indexation requested` and priority crawl queue receipt. This is a request, not completed indexing, a search impression, a click, an order, or revenue. GitHub's authoritative repository traffic still showed zero views, unique views, and referrers. Verified gross remains USD 0.00/100.00.
- 2026-07-13 indexed-commercial-URL follow-up: Search Console URL Inspection now authoritatively reports both `cat-bandana-guide.html` (the strongest measured guide intent) and `tie-on-vs-over-collar-dog-bandana.html` as on Google, indexed, and served over HTTPS. No repeat indexing request was made. Aggregate property reports were still processing, and GitHub's authoritative 14-day traffic endpoints still returned zero views, unique views, and referrers, so there is no proven impression, click, visit, order, or revenue. The public runtime remains safely closed in test mode with zero missing configuration and four false approvals (sample, supplier billing, tax, merchant account). A first probe incorrectly used nonexistent `/api/readiness`, `missing`, and `gitCommit` names; discard those outputs and use the contractually documented `/api/checkout-readiness`, `missingConfigurationCount`, and `/api/version.commit`. Verified gross remains USD 0.00/100.00.
- 2026-07-12 material-guide discovery correction: Search Console still had no query/page data and reported `dog-bandana-material-guide.html` as `Crawled - currently not indexed` after a successful Googlebot smartphone fetch, with no referring sitemap, source page, or recorded canonical in the indexed crawl. Direct public evidence contradicted a technical omission: HTTP 200, exact self-canonical, homepage link, XML sitemap entry, and robots sitemap declaration all passed; a fresh Google live test also said the URL can be indexed. Do not repeat the already-accepted indexing request. Instead, added truthful contextual links from the already-indexed cat guide and tie-on comparison to the material guide. TDD red proved both links absent before the edit. This strengthens a detected discovery path but does not prove recrawl, indexation, impression, click, order, or revenue. Verified gross remains USD 0.00/100.00.
- Material-guide discovery publication QA passed for commit `bb91057`: validation run `29220483844`, Pages run `29220483731`, and GHCR run `29220483735` all succeeded. Direct Pages probes returned HTTP 200 for both indexed source guides and found the exact material-guide link in each deployed response. This proves publication of two contextual discovery paths only, not Google recrawl, indexation, impression, click, order, or revenue.
- 2026-07-12 Bing commercial-URL follow-up: the exact verified PawSwipe property reported both `cat-bandana-guide.html` and `dog-bandana-material-guide.html` as `Discovered but not crawled`, discovered on 2026-07-12, and therefore unable to appear on Bing. Submitted each exact URL through its URL Inspection action; Bing confirmed `Indexing requested` and `Success: URL submitted successfully. We will review the URL for indexation.` This is URL-level queue evidence only, not a crawl, indexation, search/AI citation, impression, click, order, or revenue. Public readiness remains false in test mode with zero missing configuration; verified gross remains USD 0.00/100.00.
- 2026-07-12 cat-community conversion pass: a privacy-bounded Gmail search found no launch-form notification and exposed no subscriber address or response; GitHub still reported zero repository views/referrers, while Discussions `#4` and `#5` had no comments. Published Q&A Discussion `#6`, `How do you compare a cat bandana size safely?`, around the strongest measured guide intent. It states the verified 44/54/64 cm squares, comparison-first method, non-guarantee, supervised accessory limits, closed orders, exact guide, `github`-attributed storefront return, and prohibited-data boundary. Added the reciprocal cat-guide route plus the new public discussion to README and `llms.txt`, with executable coverage. This is a factual acquisition/community surface, not a subscriber, reader, comment, visit, order, or revenue. Verified gross remains USD 0.00/100.00.
- Cat-community publication QA passed for commit `f9588e2`: validation run `29220770103`, Pages run `29220770072`, and GHCR run `29220770063` all succeeded. Anonymous HTTP returned 200 for Discussion `#6`, the cat guide, and `llms.txt`; the discussion exposed its exact title and guide backlink, while the guide exposed the reciprocal route/public-data warning and `llms.txt` listed the new community surface. This proves publication and navigation only, not readership, reply, subscriber, visit, order, or revenue.
- 2026-07-12 Google recrawl follow-up: Search Console now reports `dog-bandana-material-guide.html` as on Google, indexed, and served over HTTPS after the contextual discovery links were published. This is recovery evidence for indexation only; aggregate performance still says processing and exposes no query, page, impression, or click data. The next commercial guide audit found `how-to-tie-dog-bandana.html` unknown to Google with no prior crawl. A fresh live test proved Google access and indexability, then Search Console confirmed its priority-crawl request. GitHub traffic remained zero and public readiness remained false; verified gross remains USD 0.00/100.00.
- 2026-07-12 Bing post-submission audit: the cat and material guides remain `Discovered but not crawled`; their accepted requests have not yet become crawl or index evidence. Search Performance explicitly asks to check back in 48 hours. AI Performance reports zero citations, zero cited pages, no grounding queries, and no page data. A fresh Bing Live URL test on the material guide then proved `URL can be indexed by Bing`, `No SEO/GEO issues found`, and two markup types detected. Therefore no resubmission or speculative page edit was made. This is clean live eligibility, not crawl, indexation, citation, impression, click, order, or revenue; verified gross remains USD 0.00/100.00.
- 2026-07-12 hero-conversion correction: the homepage's highest-prominence CTA sent visitors to the product/status flow even though checkout is closed, while the only actionable conversion (the consent-limited launch notice) appeared near the page end. TDD red proved the hero lacked the form route. Made `Get one launch email` the primary hero action with the exact verified Google Form, `_blank`, and `noopener noreferrer`; retained `Meet Pet Parade` as the secondary product route and allowed the action group to wrap. Local browser QA at 375x812 found exact viewport/scroll widths of 375, a 343 px action container inside the 343 px hero, correct CTA URL/target, and no overflow. At 1440x900 the primary CTA was above the fold, the secondary route remained visible, and the console had zero errors. This is a conversion-path improvement, not a subscriber, visit, order, or revenue; verified gross remains USD 0.00/100.00.

## Verified revenue ledger

| Paid date | Order ID | USD gross | Refund status | Evidence |
| --- | --- | ---: | --- | --- |
| _None_ | — | 0.00 | — | No eligible live payments; the verified Stripe test drill is excluded |

**Verified gross revenue: USD 0.00 / 100.00.**
